//para não criar várias variáveis, utilizamos os vetores
//eles são como "armários", arnazenando os dados (geralmente do mesmo tipo)

let cozinha = ["Arroz", "Feijão", "Açúcar"]
// os vetores se iniciam pelo 0
console.log(cozinha[0])

